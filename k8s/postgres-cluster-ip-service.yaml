apiVersion: v1
kind: Service
metadata:
  name: postgres-cluster-ip-service
  labels:
    service: postgres
spec:
  type: ClusterIP
  selector:
    component: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
