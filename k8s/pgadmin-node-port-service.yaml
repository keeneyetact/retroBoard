apiVersion: v1
kind: Service
metadata:
  name: pgadmin-node-port
  labels:
    nodeport: pgadmin
spec:
  type: NodePort
  ports:
    - port: 3050
      targetPort: 80
      nodePort: 30080
  selector:
    component: pgadmin
