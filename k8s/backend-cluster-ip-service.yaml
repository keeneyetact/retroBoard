apiVersion: v1
kind: Service
metadata:
  name: backend-cluster-ip-service
  labels:
    service: backend
spec:
  type: ClusterIP
  selector:
    component: backend
  ports:
    - port: 3201
      targetPort: 3201
      name: backend
